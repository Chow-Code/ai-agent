---
description: 改仓库前须确认需求并维护 docs/plans 计划；Hook 不替代本流程
alwaysApply: true
---

# 需求确认与计划先行（改代码前）

## 主流程（理论上的正确顺序）

**需求对齐 → 在 `docs/plans/` 建立或更新计划 → 再按步骤改仓库。**  
这不是靠「在对话里注入一段说明」或 Hook 的 `additional_context` 来完成的；**主 Agent 应在回复里显式完成计划步骤**（可简可详），再执行工具/写文件。Hook 只做门禁与状态侧车，**不**承担「替你决定有没有计划」。

## 与 Hook 的分工

- **`beforeSubmitPrompt` / `subagentStop` 等 Hook**：发送前侧车、子代理结束落盘等**技术侧车**；**不能**代替「确认需求」「写计划」。
- **本规则**：约束主 Agent **在对话里**先对齐需求与计划，**再**改仓库；否则会出现「Hook 跑了但直接干活」的错位。

## 何时必须先有计划

凡将使用 `write` / `search_replace` / 终端**增删改**仓库文件（含删文件、改 `docs/plans/` 以外文档）时，**须**在本轮对话中**先于动手**完成：

1. **确认需求**：与用户对齐目标、范围、验收；信息不足时**先问**，不猜。用户一句话已说清时可简要复述确认。
2. **计划**：若**尚无**与本任务对应的 **`docs/plans/*.md`**（或现有计划未覆盖本次变更），**应先**新建或更新计划，再写代码；计划在 **`docs/plans/`** 下**新建**或**更新** Markdown（可迭代）；写明步骤、涉及路径/模块、完成标准。**极小**任务也可写**极简计划**（数行即可：目标、文件路径、完成标准），但**禁止**跳过「无计划直接落文件」。
3. **再执行**：按计划中顺序改代码/删文件；**禁止**在**未**经过上述（1）（2）的情况下直接批量删除、大改。

## 例外（用户明确豁免时）

- 用户说「**不用计划，直接删/只改某文件**」等 → 须在回复里**一句话确认**后执行，仍建议记一条极简计划到 `docs/plans/` 便于追溯（可选）。
- **纯只读**（查文件、解释、不写入）不适用本节。

## 参考

- **writing-plans** Skill（若存在）：拆任务与计划结构。
- 计划目录：**`docs/plans/`**。
