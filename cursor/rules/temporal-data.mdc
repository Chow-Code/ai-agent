---
description: 时间/日期等时效性数据的防幻觉规范
alwaysApply: true
---

# 时效性数据防幻觉规范

## 问题背景

当用户询问**当前日期、当前时间**等时效性数据时，若依赖对话开始时注入的 `user_info` 中的 `Today's date`，可能得到错误结果（例如年份错误）。该静态信息可能来自缓存、不同时区或服务器时间，非用户本机实时数据。

## 核心原则

**时效性数据必须通过工具实时获取，禁止依赖预注入的静态上下文。**

## 强制要求

- ✅ **当前日期/时间**：用户询问时，必须执行 `date` 命令获取用户环境的实时时间
- ❌ **禁止**：直接使用 `user_info` 中的 `Today's date` 回答时效性问题
- ✅ **可验证性**：涉及「现在」「当前」「今天」等语义时，优先使用 run_terminal_cmd 等工具获取实时数据

## 适用场景

- 用户问「当前日期」「现在几点」「今天星期几」等
- 任务需要插入当前时间戳、日期格式等

## 修改效果

遵循此规则后，AI 在回答时效性问题时会主动调用 `date` 命令，避免因静态上下文错误导致幻觉。
